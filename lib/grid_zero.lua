local grid = {}

function grid.gkey_rec(x, y, z)

end

function grid.gdraw_rec(x, y, z)

end

function grid.gkey_cut(x, y, z)

end

function grid.gdraw_cut(x, y, z)

end

function grid.gkey_lfo(x, y, z)

end

function grid.gdraw_lfo(x, y, z)

end

function grid.gkey_env(x, y, z)

end

function grid.gdraw_env(x, y, z)

end

function grid.gkey_patterns(x, y, z)

end

function grid.gdraw_patterns(x, y, z)

end

function grid.gkey_tape(x, y, z)

end

function grid.gdraw_tape(x, y, z)

end




return grid